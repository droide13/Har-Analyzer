[project]
name = "har-analyzer-backend"
version = "0.1.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.pylint.main]
py-version = "3.14"

[tool.pylint.format]
max-line-length = 100

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
